package com.whoame.dressme.network

import retrofit2.http.GET
import retrofit2.http.Path
import retrofit2.http.Query
import rx.Observable

interface Api {

    /*@GET("/api/v1/timetables.json")
    fun getTimetable(@Query("start") lastItemId: Int, @Query("limit") limit: Int): Observable<TimetableModelList>

    @GET("/api/v1/events/{id}/user.json")
    fun getUser(@Path("id") id: String): Observable<UserInfoModel>

    @GET("/api/v1/speakers.json")
    fun getSpeakers(@Query("start") lastItemId: Int, @Query("limit") limit: Int): Observable<SpeakerModelList>*/

}